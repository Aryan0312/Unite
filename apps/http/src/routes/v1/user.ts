import  { Router } from "express";

export const userRouter:Router = Router();


// create user metadata
userRouter.post("/metadata",(req,res)=>{

})

// get user metadatas in bulk
userRouter.get("/metadata/bulk",(req,res)=>{

})